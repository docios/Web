基础
 1.数据类型
   分类
   判断
       typeof 数值/字符串/布尔/funcation,不能区别null/Object,Array/Object
       instanceof 具体对象
       === null/underfined
   相关问题
       underfined与null的区别
       什么时候将变量赋值为null
       严格区分变量类型与数据类型
 2.数据、变量、内存
   数据:存储在内存中,具有特定信息，二进制
   变量:变量名和变量值
   内存:存储空间
   三者关系:内存是一个容器,用来存储程序运行需要操作的数据
      变量时内存的标识,我们可以通过变量找到数据在内存中的存储,进而操作数据
   相关问题:
       关于赋值与内存的问题
       关于引用变量赋值问题
       js函数传递参数时,是值传递还是引用传递
 3.对象
   什么是对象
       编程的抽象
       多个数据的封装体
   为啥用对象

   对象的组成
       属性
       方法
   对象如何访问内部数据
       对象.变量名
       对象['变量名']
 4.函数
   什么是函数
       实现特定功能，多条语句的封装体
   为啥用函数--封装
   定义函数
       函数声明
       函数表达式
       函数提升与变量提升
   调用(执行)函数
       fn();
       obj.fn();
       new fn();
       fn.call(obj)//让fn临时成为obj的方法可以调用
   回调函数
       特征:
            我定义
            我没调用
            函数最终执行啦
       常见的回调函数
            dom事件
            定时器
   IIFE(匿名函数自调用)
      ()()
      作用:
         隐藏内部是实现
         不污染全局
   函数this
        以函数的形式调用时,this永远是window
        以方法的形式的对象调用时,this值的是调用方法的对象
        以构造函数的形式调用时,this就是新创建的对象.
        是用call和apply调用时,this 是指定的那个对象
        在事件的响应函数中,响应函数是谁,绑定的this就是谁

   其他问题
      分号
      空格
